import React from 'react';

// Simple placeholder provider while we fix the Reown integration
export default function ReownProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}